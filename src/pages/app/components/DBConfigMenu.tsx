// DBConfigMenu was used for manual SQLite schema setup.
// With Prisma + Postgres, schema migrations are handled via `prisma migrate dev`.
const DBConfigMenu = () => null;

export default DBConfigMenu;
